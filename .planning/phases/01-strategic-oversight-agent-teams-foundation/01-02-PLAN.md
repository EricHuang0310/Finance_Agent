---
phase: 01-strategic-oversight-agent-teams-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/strategic/cio.md
  - agents/strategic/macro_strategist.md
  - agents/strategic/eod_review.md
  - config/settings.yaml
autonomous: true
requirements: [TEAMS-02, TEAMS-03]

must_haves:
  truths:
    - "Three new agent specs exist for CIO, Macro Strategist, and EOD Review in Chinese following existing spec pattern"
    - "Each agent spec has YAML frontmatter specifying model tier"
    - "config/settings.yaml has new sections for strategic role tunables"
  artifacts:
    - path: "agents/strategic/cio.md"
      provides: "CIO agent specification in Chinese with Opus model tier"
      contains: "trading_stance"
    - path: "agents/strategic/macro_strategist.md"
      provides: "Macro Strategist spec in Chinese with Sonnet model tier"
      contains: "macro_outlook"
    - path: "agents/strategic/eod_review.md"
      provides: "EOD Review Analyst spec in Chinese with Sonnet model tier"
      contains: "eod_review"
    - path: "config/settings.yaml"
      provides: "New strategic role configuration sections"
      contains: "cio:"
  key_links:
    - from: "agents/strategic/cio.md"
      to: "src/agents_launcher.py"
      via: "task_cio_directive() function reference in execution section"
      pattern: "task_cio_directive"
    - from: "agents/strategic/macro_strategist.md"
      to: "src/agents_launcher.py"
      via: "task_macro_strategist() function reference"
      pattern: "task_macro_strategist"
    - from: "agents/strategic/eod_review.md"
      to: "src/agents_launcher.py"
      via: "task_eod_review() function reference"
      pattern: "task_eod_review"
---

<objective>
Create the three new strategic role agent specs (CIO, Macro Strategist, EOD Review) in Chinese following existing agent spec patterns, with YAML frontmatter for model tier assignment. Add corresponding configuration sections to settings.yaml.

Purpose: Agent specs are the "job descriptions" that Claude teammates read. Per D-07, all specs are in Chinese. Per D-08, they stay in `agents/` directory. Per TEAMS-02/03, each spec has model tier frontmatter. These specs must exist before task functions can be written.
Output: Three `.md` agent specs in `agents/strategic/`, updated `config/settings.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-strategic-oversight-agent-teams-foundation/01-CONTEXT.md
@.planning/phases/01-strategic-oversight-agent-teams-foundation/01-RESEARCH.md

@agents/researchers/research_judge.md
@agents/analysts/market_analyst.md
@config/settings.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strategic role agent specs with model tier frontmatter</name>
  <files>agents/strategic/cio.md, agents/strategic/macro_strategist.md, agents/strategic/eod_review.md</files>
  <read_first>agents/researchers/research_judge.md, agents/analysts/market_analyst.md, agents/analysts/sentiment_analyst.md</read_first>
  <action>
Read existing agent specs to understand the Chinese-language pattern, then create three new specs in `agents/strategic/`.

**All specs follow this pattern** (per D-07 in Chinese, per D-08 in `agents/` directory, per TEAMS-02 with YAML frontmatter):

```yaml
---
model: [tier]
tools: [list]
---
```

**1. `agents/strategic/cio.md`** — CIO (Chief Investment Officer)
- Frontmatter: `model: opus` (TEAMS-03: Opus for critical decisions)
- Role: 首席投資官 (CIO). Sets daily trading stance before any analysis runs.
- Scope per D-04: NARROW. Sets `trading_stance` (aggressive/neutral/defensive) and `risk_budget_multiplier`. Does NOT veto individual trades.
- Inputs per D-05: reads `macro_outlook.json` + market regime + yesterday's `eod_review.json`
- Per D-06: Goes live from day 1, no shadow mode.
- Include explicit quantitative stance triggers (from Research pitfall 1):
  - VIX > 30 AND yield curve inverted = defensive (multiplier: 0.6)
  - VIX < 18 AND SPY EMA20 > EMA50 > EMA200 = aggressive (multiplier: 1.3)
  - All other conditions = neutral (multiplier: 1.0)
- Output: writes `daily_directive.json` to shared_state with fields: `date`, `timestamp`, `trading_stance`, `risk_budget_multiplier`, `halt_trading` (bool), `reasoning`, `inputs_used`, `stance_triggers_met`
- Execution section: `from src.agents_launcher import task_cio_directive; result = task_cio_directive()`
- Include `halt_trading` field per CIO-02: when True, skip all trading phases

**2. `agents/strategic/macro_strategist.md`** — Macro Strategist
- Frontmatter: `model: sonnet` (analytical reasoning, not critical decision)
- Role: 宏觀策略師. Provides cross-asset market intelligence.
- Per MACRO-02: MUST use code-fetched data, NOT LLM memory. Include explicit warning: "使用程式碼取得的數據，不要使用訓練資料中的市場數據"
- Data sources: VIX (yfinance `^VIX`), TLT (Alpaca bars), UUP (Alpaca bars), yield curve (yfinance `^TNX` 10yr, `^IRX` 3mo)
- Output: writes `macro_outlook.json` to shared_state with fields: `date`, `timestamp`, `cross_asset_signals` (vix, tlt, uup, yield_curve each with value/trend/interpretation), `macro_regime_suggestion`, `key_events`, `data_freshness`
- Execution section: `from src.agents_launcher import task_macro_strategist; result = task_macro_strategist()`

**3. `agents/strategic/eod_review.md`** — EOD Review Analyst
- Frontmatter: `model: sonnet` (connecting multiple data points)
- Role: 盤後檢視分析師. Produces end-of-day P&L attribution and thesis drift detection.
- Per EOD-01: P&L attribution for all open positions
- Per EOD-02: Thesis drift detection — compare entry signals vs current technical state
- Frame as observations, NOT directives (Research pitfall 4): "今天的損益是 X 因為 Y" not "明天應該更積極"
- Output: writes `eod_review.json` to shared_state with fields: `date`, `timestamp`, `portfolio_summary` (total_pnl_today, positions_count, new_entries, exits), `position_reviews` (per position with pnl, thesis_status, character_change), `thesis_drift_alerts`, `observations`, `confidence_weight` (always 1.0 for today)
- Execution section: `from src.agents_launcher import task_eod_review; result = task_eod_review()`

Write all specs entirely in Chinese (matching existing pattern from the read specs), except for code blocks and JSON schemas which stay in English/code.
  </action>
  <verify>
    <automated>cd /Users/Liang/Documents/Finace_Agent_Team && test -f agents/strategic/cio.md && test -f agents/strategic/macro_strategist.md && test -f agents/strategic/eod_review.md && grep -l "model: opus" agents/strategic/cio.md && grep -l "model: sonnet" agents/strategic/macro_strategist.md && grep -l "model: sonnet" agents/strategic/eod_review.md && echo "PASS: all 3 specs exist with correct model tiers"</automated>
  </verify>
  <acceptance_criteria>
    - `agents/strategic/cio.md` exists with `model: opus` in YAML frontmatter, mentions `trading_stance`, `risk_budget_multiplier`, `halt_trading`, `daily_directive.json`, and `task_cio_directive`
    - `agents/strategic/macro_strategist.md` exists with `model: sonnet`, mentions `macro_outlook.json`, `task_macro_strategist`, and explicit warning against using LLM training data
    - `agents/strategic/eod_review.md` exists with `model: sonnet`, mentions `eod_review.json`, `task_eod_review`, thesis drift, and frames output as observations
    - All three specs are written in Chinese with code blocks in English
  </acceptance_criteria>
  <done>Three strategic agent specs exist in `agents/strategic/` with YAML frontmatter model tiers, Chinese language, embedded execution code references, and JSON output schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Add strategic role configuration to settings.yaml</name>
  <files>config/settings.yaml</files>
  <read_first>config/settings.yaml</read_first>
  <action>
Add new configuration sections to `config/settings.yaml` for the three strategic roles. Place after the existing `memory:` section (or at the end of the file).

Add the following YAML sections:

```yaml
# ══════════════════════════════════════════════
# Strategic Oversight Configuration
# ══════════════════════════════════════════════

cio:
  # Stance triggers (quantitative thresholds)
  vix_defensive_threshold: 30      # VIX above this = consider defensive
  vix_aggressive_threshold: 18     # VIX below this = consider aggressive
  # Risk budget multipliers per stance
  aggressive_multiplier: 1.3
  neutral_multiplier: 1.0
  defensive_multiplier: 0.6
  # Halt trading conditions
  halt_on_vix_above: 40            # Emergency halt threshold

macro:
  # Cross-asset tickers
  vix_ticker: "^VIX"
  tlt_ticker: "TLT"
  uup_ticker: "UUP"
  yield_10y_ticker: "^TNX"
  yield_3m_ticker: "^IRX"
  # Data lookback
  trend_lookback_days: 20
  sma_short: 5
  sma_long: 20

eod_review:
  # Confidence decay for historical EOD insights
  decay_weights:
    1: 1.0    # yesterday
    2: 0.5    # 2 days ago
    3: 0.25   # 3 days ago
  max_lookback_days: 3
  # Thesis drift detection
  drift_rsi_threshold: 15          # RSI change from entry exceeding this flags drift
  drift_price_reversal_pct: 5      # Price reversal from entry exceeding this flags drift

# Model tier assignments (TEAMS-03)
model_tiers:
  # Strategic (CIO is Lead agent, uses default model)
  macro-strategist: sonnet
  eod-review: sonnet
  # Analysts
  symbol-screener: haiku
  market-analyst: haiku
  technical-analyst: haiku
  sentiment-analyst: haiku
  fundamentals-analyst: haiku
  # Debate
  bull-researcher: sonnet
  bear-researcher: sonnet
  research-judge: opus
  # Risk + Execution
  risk-manager: haiku
  executor: haiku
  position-reviewer: haiku
  # Post-trade
  reporter: haiku
  reflection-analyst: opus
```

Do NOT modify any existing configuration sections. Only append new sections.
  </action>
  <verify>
    <automated>cd /Users/Liang/Documents/Finace_Agent_Team && python -c "import yaml; cfg=yaml.safe_load(open('config/settings.yaml')); assert 'cio' in cfg, 'missing cio'; assert 'macro' in cfg, 'missing macro'; assert 'eod_review' in cfg, 'missing eod_review'; assert 'model_tiers' in cfg, 'missing model_tiers'; assert cfg['model_tiers']['research-judge']=='opus'; assert cfg['cio']['aggressive_multiplier']==1.3; print('PASS: settings.yaml has all new sections')"</automated>
  </verify>
  <acceptance_criteria>
    - `config/settings.yaml` contains `cio:` section with stance thresholds and multiplier values
    - `config/settings.yaml` contains `macro:` section with ticker symbols and lookback settings
    - `config/settings.yaml` contains `eod_review:` section with decay_weights and drift thresholds
    - `config/settings.yaml` contains `model_tiers:` section mapping all agent roles to haiku/sonnet/opus
    - Existing sections (watchlist, scoring, risk, decision, etc.) are unchanged
    - File parses without error via `yaml.safe_load()`
  </acceptance_criteria>
  <done>settings.yaml has configuration sections for all three strategic roles plus a model_tiers map covering every agent in the system.</done>
</task>

</tasks>

<verification>
- All three agent spec files exist in `agents/strategic/`
- Each spec has YAML frontmatter with model tier
- `config/settings.yaml` loads without error and has new sections
- Existing config sections unchanged: `python -c "import yaml; cfg=yaml.safe_load(open('config/settings.yaml')); assert 'watchlist' in cfg; print('existing config intact')"`
</verification>

<success_criteria>
- TEAMS-02: Three new agent specs have YAML frontmatter with model tier, tools, and permissions
- TEAMS-03: model_tiers map in settings.yaml assigns Opus to CIO/Judge/Reflection, Sonnet to analysts/debate/macro/eod, Haiku to data collection/execution
- All specs in Chinese per D-07, in agents/ directory per D-08
- Configuration tunables for CIO thresholds, macro tickers, EOD decay weights all in settings.yaml
</success_criteria>

<output>
After completion, create `.planning/phases/01-strategic-oversight-agent-teams-foundation/01-02-SUMMARY.md`
</output>
