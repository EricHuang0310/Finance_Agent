---
phase: 03-debate-execution-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/researchers/sector_specialist.md
  - src/debate/helpers.py
  - config/settings.yaml
  - src/agents_launcher.py
  - src/team_orchestrator.py
autonomous: true
requirements: [SECT-01, SECT-02, SECT-03]

must_haves:
  truths:
    - "During investment debate, sector intelligence is available for each Top-N candidate"
    - "Sector Specialist provides supply chain dynamics, sector rotation signals, and competitive landscape"
    - "Bull, Bear, and Judge all read sector intelligence as part of debate context"
  artifacts:
    - path: "agents/researchers/sector_specialist.md"
      provides: "Sector Specialist agent spec in Chinese"
      contains: "sector_specialist"
    - path: "src/debate/helpers.py"
      provides: "_fetch_sector_intelligence function enriching debate context"
      contains: "sector_intelligence"
    - path: "config/settings.yaml"
      provides: "sector_specialist configuration section"
      contains: "sector_specialist"
    - path: "src/agents_launcher.py"
      provides: "task_sector_specialist function"
      contains: "task_sector_specialist"
    - path: "src/team_orchestrator.py"
      provides: "Sector Specialist phase in team prompt"
      contains: "sector_specialist"
  key_links:
    - from: "src/debate/helpers.py"
      to: "shared_state/debate_context_{symbol}.json"
      via: "sector_intelligence field in context dict"
      pattern: "sector_intelligence"
    - from: "src/agents_launcher.py"
      to: "src/debate/helpers.py"
      via: "task_sector_specialist calls _fetch_sector_intelligence"
      pattern: "task_sector_specialist"
---

<objective>
Add Sector Specialist as a 4th voice in investment debates by enriching debate context with deep sector intelligence (supply chain, rotation signals, competitive landscape) before Bull/Bear/Judge run.

Purpose: Bull/Bear/Judge currently debate without sector-specific knowledge. Adding sector intelligence improves debate quality by grounding arguments in supply chain dynamics, peer performance, and sector catalysts.
Output: Sector Specialist agent spec, sector intelligence enrichment in debate context, task function, pipeline integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-debate-execution-enhancement/03-CONTEXT.md
@.planning/phases/03-debate-execution-enhancement/03-RESEARCH.md

@src/debate/helpers.py
@agents/researchers/bull_researcher.md
@config/settings.yaml
@src/agents_launcher.py
@src/team_orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sector Specialist agent spec and sector intelligence function</name>
  <files>agents/researchers/sector_specialist.md, src/debate/helpers.py, config/settings.yaml</files>
  <read_first>
    - agents/researchers/bull_researcher.md (reference spec format -- Chinese, with execution code and I/O schema)
    - agents/researchers/bear_researcher.md (reference spec format)
    - src/debate/helpers.py (existing debate context preparation -- add sector enrichment here)
    - config/settings.yaml (add sector_specialist config section)
    - src/analysis/fundamentals.py (reference for yfinance usage pattern)
  </read_first>
  <action>
1. Create `agents/researchers/sector_specialist.md` in Chinese following the same format as bull_researcher.md and bear_researcher.md. The spec must define:
   - Role: Sector domain expert providing supply chain intelligence, sector rotation signals, and competitive landscape analysis
   - Model tier: sonnet (same as Bull/Bear researchers per D-07/P1)
   - Explicit scope boundary: "You provide ONLY supply chain risks, sector rotation signals, and competitive landscape. You do NOT evaluate whether to buy or sell -- that is Bull/Bear/Judge's job."
   - Input: `debate_context_{symbol}.json` (reads sector_intelligence field)
   - Output: Sector Specialist does NOT write its own output file. Its intelligence is embedded in debate_context by the code function. The spec describes what intelligence the LLM should look for when sector data is insufficient from code alone.
   - Execution code: `from src.agents_launcher import task_sector_specialist; task_sector_specialist(symbol)`

2. Add `_fetch_sector_intelligence(symbol, context)` function to `src/debate/helpers.py`. This function:
   - Uses `yfinance.Ticker(symbol).info` to get sector, industry, marketCap
   - Reads `fundamentals_signals.json` from STATE_DIR for pe_ratio, revenue_growth
   - Reads `technical_signals.json` from STATE_DIR to compare symbol trend vs sector peers
   - Reads `market_overview.json` for market regime context
   - Returns a dict with structure:
     ```python
     {
         "sector": str,
         "industry": str,
         "market_cap": float | None,
         "supply_chain": {
             "key_context": str,  # LLM training knowledge fills gaps
             "demand_signals": str | None,
         },
         "sector_rotation": {
             "sector_momentum": str,  # Derived from technical data
             "relative_strength": float | None,
         },
         "competitive_landscape": {
             "pe_ratio": float | None,
             "revenue_growth": float | None,
             "peer_context": str,  # Summary of peer data if available
         },
     }
     ```
   - Wrap entire function in try/except returning `{"error": str(e)}` on failure (D-12 graceful degradation)
   - Import yfinance with `try/except ImportError` using `_HAS_YFINANCE` flag pattern (consistent with existing codebase)

3. Modify `task_prepare_debate_context()` in `src/debate/helpers.py` to call `_fetch_sector_intelligence(symbol, context)` and add the result as `context["sector_intelligence"]` BEFORE saving the context file. Place it after the fundamentals loading block (line ~80) and before memory search (line ~93).

4. Add `sector_specialist` section to `config/settings.yaml` after the `debate` section:
   ```yaml
   sector_specialist:
     enabled: true
     data_sources:
       - yfinance        # Sector/industry from Ticker.info
       - fundamentals    # PE, revenue growth from fundamentals_signals.json
       - technical       # Relative strength from technical_signals.json
   ```
  </action>
  <verify>
    <automated>python -c "from src.debate.helpers import _fetch_sector_intelligence; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - agents/researchers/sector_specialist.md exists and contains Chinese text with "sector_specialist" and "供應鏈" (supply chain)
    - src/debate/helpers.py contains def _fetch_sector_intelligence and context["sector_intelligence"] in task_prepare_debate_context
    - config/settings.yaml contains a "sector_specialist:" section with "enabled: true"
  </acceptance_criteria>
  <done>Sector Specialist agent spec exists, sector intelligence function enriches debate context, config section added</done>
</task>

<task type="auto">
  <name>Task 2: Wire Sector Specialist into pipeline and team orchestrator</name>
  <files>src/agents_launcher.py, src/team_orchestrator.py</files>
  <read_first>
    - src/agents_launcher.py (existing task_prepare_debate function at ~line 721, and task_fundamentals_analyst at ~line 712)
    - src/team_orchestrator.py (Phase Group 3 debate section -- add sector step)
    - src/debate/helpers.py (verify _fetch_sector_intelligence exists from Task 1)
  </read_first>
  <action>
1. Add `task_sector_specialist(symbol: str) -> dict` function to `src/agents_launcher.py` near the debate functions (~line 735). This function:
   - Calls `_fetch_sector_intelligence(symbol, {})` from `src.debate.helpers`
   - Prints status: `f"🏭 [Sector Specialist] ✅ Sector intelligence for {symbol}"`
   - Returns the sector intelligence dict
   - Wraps in try/except, printing warning and returning empty dict on failure (D-12)
   - Note: This task function is called by the Lead agent BEFORE `task_prepare_debate()`. The intelligence is then picked up when `task_prepare_debate_context()` runs (since it calls `_fetch_sector_intelligence` internally).
   - Actually, since `_fetch_sector_intelligence` is already called inside `task_prepare_debate_context()` (added in Task 1), `task_sector_specialist` serves as an explicit pipeline step that can also be called standalone. It should save sector intelligence to `{STATE_DIR}/sector_intelligence_{symbol}.json` via `save_state_atomic` so it is available even if debate prep runs separately.

2. Update `task_prepare_debate_context()` call flow: In `_fetch_sector_intelligence`, first check if `{STATE_DIR}/sector_intelligence_{symbol}.json` already exists (pre-computed by task_sector_specialist). If yes, load and return it instead of re-fetching. This deduplicates work.

3. Update `src/team_orchestrator.py` `build_team_prompt()` function, in the Phase Group 3 (Investment Debate) section. Add a sector intelligence step BEFORE the Bull/Bear debate spawning:
   ```
   ### Phase Group 3: Investment Debate (Top-N candidates)
   對每個 Top-N candidate (symbol):
   
   1. **Sector Intelligence** (Lead直接執行):
      ```python
      from src.agents_launcher import task_sector_specialist
      sector_data = task_sector_specialist(symbol)
      ```
   
   2. **Debate Prep** (Lead直接執行):
      [existing task_prepare_debate code]
   ```
   Keep the existing Bull/Bear/Judge spawning unchanged. The sector intelligence flows through debate_context_{symbol}.json.

4. Add the sector_specialist model tier to the model_tiers reference in settings.yaml if not already present:
   ```yaml
   model_tiers:
     sector-specialist: sonnet
   ```
  </action>
  <verify>
    <automated>python -c "from src.agents_launcher import task_sector_specialist; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/agents_launcher.py contains "def task_sector_specialist" function
    - src/team_orchestrator.py contains "task_sector_specialist" in the Phase Group 3 section
    - Sector intelligence step appears BEFORE Bull/Bear debate spawning in team prompt
  </acceptance_criteria>
  <done>Sector Specialist is wired into the pipeline: task function exists, team orchestrator includes sector step before debate</done>
</task>

</tasks>

<verification>
- `python -c "from src.debate.helpers import _fetch_sector_intelligence, task_prepare_debate_context; print('debate helpers ok')"` passes
- `python -c "from src.agents_launcher import task_sector_specialist, task_prepare_debate; print('launcher ok')"` passes
- `grep -q 'sector_intelligence' src/debate/helpers.py` returns 0
- `grep -q 'task_sector_specialist' src/agents_launcher.py` returns 0
- `grep -q 'sector_specialist' src/team_orchestrator.py` returns 0
- `grep -q 'sector_specialist' config/settings.yaml` returns 0
- `test -f agents/researchers/sector_specialist.md` returns 0
</verification>

<success_criteria>
- Sector Specialist agent spec exists in agents/researchers/
- Debate context includes sector_intelligence field with sector, industry, supply chain, rotation, competitive landscape data
- Pipeline team prompt includes sector intelligence step before debate spawning
- All imports resolve without error
</success_criteria>

<output>
After completion, create `.planning/phases/03-debate-execution-enhancement/03-01-SUMMARY.md`
</output>
